import { ScaleBlockDirective } from './scale-block.directive';

describe('ScaleBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleBlockDirective();
    expect(directive).toBeTruthy();
  });
});
